# Aetheris Offline RPG (Codex-ready)

Projeto vanilla (HTML + Canvas + JS), sem dependências.

Rodar offline:
- Abra `index.html` direto (file://).
- Se o navegador limitar storage, rode um servidor local:
  - `python -m http.server 8000`
  - Abra `http://localhost:8000`

Dados:
- `src/data.js` (classes/itens/monstros/quests/mapas)
- `src/game.js` (motor)
